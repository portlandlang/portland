#!/bin/sh
# Benchmark the seed and the compiler (#25) — the harness itself is Ruby, and
# selecting a Ruby is the script's job, not the caller's (see ruby.sh).
set -e
cd "$(dirname "$0")/.."
. script/docs/lib/ruby.sh
exec ruby script/bench.rb "$@"
