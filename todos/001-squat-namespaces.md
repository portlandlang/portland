# Squat the namespaces

The name is only ours once the handles are. All checked available as of 2026-07-15.

## Tasks

- [x] GitHub org: `portlandlang` — the primary (web UI only — no API for org creation)
- [x] GitHub org: `pdxlang` — defensive squat; bio + profile README (`pdxlang/.github`) point to `portlandlang`
- [x] crates.io: published placeholder crate `portland` v0.0.0 (bare `pdx` is a reserved placeholder, skip)
- [x] Domains: extracted to [017](017-register-domains.md)

## Notes

- **Decided:** primary org is `portlandlang`; `pdxlang` is a redirect squat.
- Bare `portland`/`pdx` GitHub names are taken (as users; same namespace as orgs).
- An org profile README lives in a repo named `.github` at `profile/README.md` — so the `pdxlang` redirect needs that one repo. `portlandlang` stays empty for now.
