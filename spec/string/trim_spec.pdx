# The whitespace trimmers — none built yet, held pending (#23).
#
# From ruby/spec core/string/{strip,lstrip,rstrip,chomp}_spec.rb.

require_relative "../spec_helper"

context "String" do
  describe "the trimmers" do
    pending "String#strip — no builtin yet" do
      expect("  rose  ".strip).to eq("rose")
    end

    pending "String#lstrip and #rstrip — no builtins yet" do
      expect("  rose".lstrip).to eq("rose")
      expect("rose  ".rstrip).to eq("rose")
    end

    pending "String#chomp — no builtin yet; one trailing newline" do
      expect("rose\n".chomp).to eq("rose")
      expect("rose".chomp).to eq("rose")
    end
  end
end
